---
group: koagi
state:
    default: 테마별공간탐방-국립정원문화원
    type2: 테마별공간탐방-학습/체험-국립정원문화원
    type3: 테마별공간탐방-탐구/전문-국립정원문화원
---
<!DOCTYPE html>
<html lang="ko">
<head>
    <%
    currentName = '국립정원문화원';
    currentCategory = 'garden';
    currentNav = 'tour';
    imgPath = `${process.env.IMG_PATH}/koagi`
    %>
    <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_head`, {
        title: currentName,
        currentCategory: currentCategory
    }); %>
</head>
<body class="<%= currentCategory %>">
    <!-- skip navigation -->
    <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_skipNavigation`) %>
    <!-- // skip navigation -->

    <!-- wrap -->
    <div id="wrap">
        <!-- header -->
        <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_header`, {
            currentCategory,
            currentNav,
            currentName
        }) %>
        <!-- // header -->

        <!-- visual sub -->
        <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_visualSub`, {
            type: 'plant',
            currentNav: '둘러보기',
            key: '국민이 행복한 수목원',
            info: '산림생물다양성 보전과 활용의 중심인 국립백두대간수목원에서<br>산림가치 증진을 통해 국민이 행복한 수목원을 만들어 나가겠습니다',
        }) %>
        <!-- // visual sub -->

        <!-- title -->
        <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_titleSub`, {
            title: '테마별 공간 탐방',
            depths: [
                '둘러보기',
                '테마별 공간 탐방'
            ],
        }) %>
        <!-- // title -->

        <!-- contents -->
        <div id="body">
            <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_guideMap`, {
            }) %>
            <%
            const currentGuide = guide[nav[currentCategory]];
            const pages = currentGuide.pages;
            const titles = pages.map(page => page.title);

            const pagesObj = {
                default: 0,
                type2: 1,
                type3: 2,
            };

            const currentIndex = pagesObj[state];
            const currentPages = pages[currentIndex];
            const title = currentPages.title;
            const items = currentPages.items;
            const desc = currentPages.desc;
            %>

            <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_tab`, {
                items: titles,
                current: currentIndex
            }) %>
            
            <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_themeDetail`, {
                title,
                desc,
                items,
            }) %>


        </div>
        <!-- // contents -->

        <!-- footer -->
        <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_footer`, {
            currentCategory,
            currentName,
        }) %>
        <!-- // footer -->

        <!-- navigation -->
        <%- include(`${process.env.INCLUDE_PATH}/koagi/partials/_navigation`, {
            currentCategory: currentCategory
        }) %>
        <!-- // navigation -->
    </div>
    <!-- // wrap -->
</body>
</html>