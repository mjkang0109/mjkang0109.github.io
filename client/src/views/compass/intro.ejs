---
group: compass
state:
    default: 국제통용 발자국 검증제도란
---
<!DOCTYPE html>
<html lang="ko">
<%- include(`${process.env.INCLUDE_PATH}/compass/partials/_constants`, {}); %>
<%- include(`${process.env.INCLUDE_PATH}/compass/partials/_head`, {}); %>
<body>
    <div class="wrap">
        <!-- 상단메뉴영역 시작 -->
        <%- include(`${process.env.INCLUDE_PATH}/compass/partials/_header`, {}); %>
        <!--모바일 MENUAREA-->
        <%- include(`${process.env.INCLUDE_PATH}/compass/partials/_mobileHeader`, {}); %>
        <div class="totladim" style="display: none; opacity: 0.8;"></div>
        <!-- 상단메뉴영역 끝 -->
        <div class="container">
            <!-- 타이틀영역 시작 -->
            <div class="sub-visual7">
                <div class="inner7">
                    <h2><%= pageTitle %></h2>
                    <div class="print-box">
                        <!-- <button><img src="<%= path.images %>/image/site/link.svg"></button> -->
                    </div>
                </div>
            </div>
            <!-- 타이틀영역 끝 -->
            <div class="inner sub-contents-box">
                <!-- 왼쪽메뉴영역 시작 -->
                <%- include(`${process.env.INCLUDE_PATH}/compass/partials/_leftNav`, {
                    currentIndex: 0
                }); %>
                <!-- 왼쪽메뉴영역 끝 -->
                <div class="sub-contents">
                    <div class="right-fixed">
                        <div class="right-top">
                            <a href=""><span>TOP</span></a>
                        </div>
                    </div>
                    <%- include(`${process.env.INCLUDE_PATH}/compass/partials/_breadCrum`, {
                    depth1: '국제통용 발자국 검증제도',
                    depth2: '제도소개',
                        depth3: pageTitle,
                    }); %>
                    <div class="lci_area mt-30">
                        <h3><%= pageTitle %></h3>
                    </div>

                    <div class="lcidb-about bg-ipef">
                        <strong class="about-title">국제통용 발자국 검증제도란?</strong>
                        <ul class="about-standards">
                            <li class="about-standard">전과정평가 방법을 이용하여 산정한 제품의 환경정보(탄소발자국, 물발자국 등)를 신뢰성 및 일관성 있는 적합성평가(검증) 서비스를 통해 이해관계자에게 정보를 전달하도록 지원하는 제도입니다.</li>
                            <li class="about-standard">한국인정기구(KOLAS) 공인 검증기관이 검증을 수행하고 검증의견서를 발급합니다.</li>
                            <li class="about-standard">글로벌 탄소규제 및 고객사의 탄소배출량 검증 요구에 대응할 수 있도록 지원하며, 향후 국제상호인정 추진과 해외 제도와의 양자협정을 적극적으로 추진하여 국제적으로 통용될 수 있도록 노력하고 있습니다.</li>
                        </ul>
                    </div>

                    <%
                    const ipef = [
                        {
                            title: '저비용 속성취득',
                            desc: '국내에서 검증을 진행하므로 심사원 경비, 수수료 등 비용절감 및 시간 단축 가능',
                            class: 'leaf'
                        },
                        {
                            title: '해외기준 검증취득',
                            desc: '현지 국제 통용되는 기준으로 검증을 취득하므로 수출기업이 글로벌 탄소규제 대응 부담 절감',
                            class: 'circle'
                        },
                        {
                            title: '기업수요 충족',
                            desc: '글로벌 고객사 요구에 대응해야 하는 수출기업에 신뢰성이 제고된 대응방안 제시',
                            class: 'factory'
                        }
                    ];
                    %>

                    <ul class="ipef-elements">
                        <% ipef.forEach((item, index) => { %>
                            <li class="visual-<%= item.class %>">
                                <strong class="ipef-title"><%= item.title %></strong>
                                <p class="ipef-description"><%= item.desc %></p>
                            </li>
                        <% }); %>
                    </ul>

                    <%
                    const history = [
                        {
                            year: '2013',
                            desc: '수출제품에 대한 “해외 탄소라벨링 인증제도” 시행',
                        },
                        {
                            year: '2015',
                            desc: '“국제통용 발자국 검증제도”로 확대 시행',
                        },
                        {
                            year: '2017',
                            desc: '해외 제도(스웨덴 International EPD)와 업무협약(MOU) 체결',
                        },
                        {
                            year: '2022',
                            desc: '<strong class="month">06월</strong>6월 산업통상자원부 국가기술표준원 한국인정기구(KOLAS)와 업무협약(MOU) 체결',
                        },
                        {
                            year: '2022',
                            desc: '<strong class="month">07월</strong>KOLAS 공인 “국제통용 발자국 검증제도” 시행',
                        },
                        {
                            year: '2024',
                            desc: '<strong class="month">11월</strong>이탈리아탄소발자국(CFI)와 상호인정협정 체결',
                        }
                    ]
                    %>
                    <div class="ipef-history">
                        <div class="history-left">
                            <strong class="history-title">연혁</strong>
                            <p class="history-description">국제통용발자국 검증제도의<br>걸어온 길을 안내합니다</p>
                        </div>
                        <ol class="histories">
                            <% history.forEach((item, index) => { %>
                                <li class="history-item">
                                    <span class="year"><%= item.year %></span>
                                    <span class="description"><%- item.desc %></span>
                                </li>
                            <% }); %>
                        </ol>
                    </div>
                </div>


            </div>
        </div>
        <!-- 푸터영역 시작 -->
        <%- include(`${process.env.INCLUDE_PATH}/compass/partials/_footer`, {}); %>
        <%- include(`${process.env.INCLUDE_PATH}/compass/partials/_layers`, {}); %>
        <!-- 푸터영역 끝 -->
    </div>
</body>
</html>
