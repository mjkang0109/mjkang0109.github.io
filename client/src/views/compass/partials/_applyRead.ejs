<%
var panel = panel ?? 0;
%>
<% if (panel === 0 || panel === 2) { %>
    <strong class="form-title">도안 사용을 위한 세부정보</strong>

    <ul class="form-items">
        <li>
            <label for="form-product-<%= panel + 1; %>"
                   class="require">검증 받은 제품명</label>
            <input id="form-product-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input"
                   placeholder="(해당되는 경우, 모델명 및 모델번호 등)"
                   required>
        </li>
        <li>
            <label for="form-type-<%= panel + 1; %>"
                   class="require">제품 유형</label>
            <input id="form-type-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input"
                   required>
        </li>
        <li>
            <label for="form-function-<%= panel + 1; %>"
                   class="require">제품 기능(제품 설명)</label>
            <input id="form-function-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input"
                   required>
        </li>
        <li>
            <label for="form-unit-<%= panel + 1; %>"
                   class="require">제품의 기능(선언)단위</label>
            <input id="form-unit-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input"
                   required>
        </li>
        <li>
            <label for="form-rule-<%= panel + 1; %>"
                   class="require">제품범주 규칙명</label>
            <input id="form-rule-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input"
                   required>
        </li>
        <li>
            <label for="form-category-<%= panel + 1; %>"
                   class="require">검증유형 및 검증기관명</label>
            <input id="form-category-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input"
                   required>
            <label for="form-date-<%= panel + 1; %>">검증번호 및 검증일자</label>
            <input id="form-date-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input">
        </li>
        <li>
            <label for="form-system-gate-<%= panel + 1; %>"
                   class="require">전과정 단계를 포함한 시스템 경계</label>
            <input type="radio"
                   id="form-system-gate-<%= panel + 1; %>"
                   name="radio-system-<%= panel + 1; %>"
                   disabled
                   checked>
            <label for="form-system-gate-<%= panel + 1; %>">
                Cradle to Gate
            </label>
            <input type="radio"
                   id="form-system-grave-<%= panel + 1; %>"
                   disabled
                   name="radio-system-<%= panel + 1; %>">
            <label for="form-system-grave-<%= panel + 1; %>">
                Cradle to Grave
            </label>
        </li>
        <li>
            <label for="form-info-<%= panel + 1; %>-total"
                   class="require">기능 또는 선언단위당 정량적 발자국 정보</label>
            <div class="column">
                <label for="form-info-<%= panel + 1; %>-total">
                    총 발자국 값
                </label>
                <span class="column-cell">
                    <input type="text"
                           readonly
                           value=""
                           id="form-info-<%= panel + 1; %>-total">
                    <span class="icon-dot">.</span>
                    <label for="form-info-<%= panel + 1; %>-1-suffix">
                        <input type="text"
                               readonly
                               value=""
                               id="form-info-<%= panel + 1; %>-1-suffix">
                    </label>
                    <span class="unit">g CO2 e/FU 또는 DU</span>
                </span>
                <label for="form-info-<%= panel + 1; %>-step">
                    제조단계<br> 발자국
                </label>
                <span class="column-cell">
                    <input type="text"
                           readonly
                           value=""
                           id="form-info-<%= panel + 1; %>-step">
                    <span class="icon-dot">.</span>
                    <label for="form-info-<%= panel + 1; %>-2-suffix">
                        <input type="text"
                               readonly
                               value=""
                               id="form-info-<%= panel + 1; %>-2-suffix">
                    </label>
                    <span class="unit">g CO2 e/FU 또는 DU</span>
                </span>
            </div>
            <div class="column">
                <label for="form-info-<%= panel + 1; %>-upstream">
                    업스트림<br>단계 발자국
                </label>
                <span class="column-cell">
                    <input type="text"
                           readonly
                           value=""
                           id="form-info-<%= panel + 1; %>-upstream">
                    <span class="icon-dot">.</span>
                    <label for="form-info-<%= panel + 1; %>-3-suffix">
                        <input type="text"
                               readonly
                               value=""
                               id="form-info-<%= panel + 1; %>-3-suffix">
                    </label>
                    <span class="unit">g CO2 e/FU 또는 DU</span>
                </span>
                <label for="form-info-<%= panel + 1; %>-downstream">
                    다운스트림<br>단계 발자국
                </label>
                <span class="column-cell">
                    <input type="text"
                           readonly
                           value=""
                           id="form-info-<%= panel + 1; %>-downstream">
                    <span class="icon-dot">.</span>
                    <label for="form-info-<%= panel + 1; %>-4-suffix">
                        <input type="text"
                               readonly
                               value=""
                               id="form-info-<%= panel + 1; %>-4-suffix">
                    </label>
                    <span class="unit">g CO2 e/FU 또는 DU</span>
                </span>
            </div>
        </li>
        <li>
            <label for="form-statistics-<%= panel + 1; %>"
                   class="require">발자국 산정을 위한 데이터 수집기간</label>
            <div class="search-period">
                <input type="text"
                       id="form-statistics-<%= panel + 1; %>"
                       disabled
                       value="2024-01-01"
                       class="input-datepicker">
                <span class="icon-wave">~</span>
                <label for="form-statistics-end-<%= panel + 1; %>">
                    <input type="text"
                           disabled
                           value="2024-01-01"
                           id="form-statistics-end-<%= panel + 1; %>"
                           class="input-datepicker">
                </label>
            </div>
        </li>
        <li>
            <label for="form-PCR-<%= panel + 1; %>"
                   class="require">기타 PCR에서 규정한 정보</label>
            <input id="form-PCR-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input"
                   required>
        </li>
    </ul>

    <div class="apply-signature">
        <ul>
            <li>「국제통용 발자국 검증제도 운영요령」제32조에 따라 도안 사용을 신청합니다.</li>
            <li>- 본 기관은 국제통용 발자국 검증제도 운영요령」제18조에 따른 발자국 정보 및 상기 정보를 한국생산기술연구원이 웹사이트 등을 통해 모든 이해관계자에게 공개하는 것에 동의함</li>
            <li>- 본 기관이 제공하는 검증받은 제품의 발자국 정보의 모든 내용은 완전하며 사실임을 확인함</li>
        </ul>
        <div class="date">
            <label class="year">20
                <input type="text"
                       readonly
                       value="12">년
            </label>
            <label class="month">
                <input type="text"
                       readonly
                       value="2">월
            </label>
            <label class="month">
                <input type="text"
                       readonly
                       value="3">일
            </label>
        </div>
        <div class="signature">
            <strong>한국생산기술연구원장 귀하</strong>
            <div class="stamp">
                <strong>신청인 (서명 또는 인)
                    <button type="button" disabled>인증완료</button>
                </strong>
                <strong>신청기관장 (직인)</strong>
            </div>
        </div>
    </div>

    <% if (panel === 2) { %>
        <strong class="form-subtitle">해외 검증제도 도안 사용 신청서(영문 작성 필요)</strong>
        <ul class="form-items">
            <li>
                <label for="form-name-<%= panel + 1 %>">Producer name <br>(제작자 성명)</label>
                <input id="form-name-<%= panel + 1 %>"
                       type="text"
                       readonly
                       value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                       class="form-input">
            </li>
            <li>
                <label for="form-designation-<%= panel + 1 %>">Commercial name of the product <br>(제품의 상업적
                    명칭)</label>
                <input id="form-designation-<%= panel + 1 %>"
                       type="text"
                       readonly
                       value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                       class="form-input">
            </li>
            <li>
                <label for="form-type-<%= panel + 1 %>">Product type <br>(제품유형)</label>
                <input id="form-type-<%= panel + 1 %>"
                       type="text"
                       readonly
                       value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                       class="form-input">
            </li>
            <li>
                <label for="form-explanation-<%= panel + 1 %>">Product description <br>(제품설명)</label>
                <input id="form-explanation-<%= panel + 1 %>"
                       type="text"
                       readonly
                       value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                       class="form-input">
            </li>
            <li>
                <label for="form-number-<%= panel + 1 %>">Registration number <br>(등록번호)</label>
                <input id="form-number-<%= panel + 1 %>"
                       type="text"
                       readonly
                       value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                       class="form-input">
            </li>
            <li>
                <label for="form-link-<%= panel + 1 %>">Registration web-link <br>(등록 웹링크)</label>
                <input id="form-link-<%= panel + 1 %>"
                       type="text"
                       readonly
                       value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                       class="form-input">
            </li>
        </ul>
        <strong class="caution">Please attach the company logo, a product picture (in jpg,
            .PNG format) and the verificationstatement issued by the verification body.
            <br>(회사 로고, 제품 사진(jpg, .PNG 형식) 및 인증 기관에서 발급한 인증 명세서를 첨부해 주시기 바랍니다.)
        </strong>
    <% } %>
<% } %>
<% if (panel === 1) { %>
    <strong class="form-title">검증제도 도안 사용 신청서</strong>

    <ul class="form-items">
        <li>
            <label for="form-certification-<%= panel + 1; %>"
                   class="require">국내도안 승인번호</label>
            <input id="form-certification-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input red"
                   placeholder="신청자가 승인번호 직접 입력 (예:25687896)"
                   required>
        </li>
    </ul>

    <strong class="form-subtitle">해외 검증제도 도안 사용 신청서(영문 작성 필요)</strong>
    <ul class="form-items">
        <li>
            <label for="form-name-<%= panel + 1; %>">Producer name (제작자 성명)</label>
            <input id="form-name-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input">
        </li>
        <li>
            <label for="form-designation-<%= panel + 1; %>">Commercial name of the product<br>(제품의 상업적 명칭)</label>
            <input id="form-designation-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input">
        </li>
        <li>
            <label for="form-type-<%= panel + 1; %>">Product type<br>(제품유형)</label>
            <input id="form-type-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input">
        </li>
        <li>
            <label for="form-explanation-<%= panel + 1; %>">Product description<br>(제품설명)</label>
            <input id="form-explanation-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input">
        </li>
        <li>
            <label for="form-number-<%= panel + 1; %>">Registration number<br>(등록번호)</label>
            <input id="form-number-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input">
        </li>
        <li>
            <label for="form-link-<%= panel + 1; %>">Registration web-link<br>(등록 웹링크)</label>
            <input id="form-link-<%= panel + 1; %>"
                   type="text"
                   readonly
                   value="숙련도 시험에 대한 정의, 운영 및 사후관리"
                   class="form-input">
        </li>
    </ul>
    <strong class="caution">Please attach the company logo, a product picture (in jpg, .PNG format) and the
        verificationstatement issued by the verification body.
        <br>(회사 로고, 제품 사진(jpg, .PNG 형식) 및 인증 기관에서 발급한 인증 명세서를 첨부해 주시기 바랍니다.)
    </strong>
<% } %>